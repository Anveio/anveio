# Runbook Retired

We no longer manage post artefacts in S3 or rotate IAM keys for that workflow. The previous helper scripts have been removed alongside the Convex post compositor.

Should a new service require AWS key rotation, document it with a fresh runbook tailored to that system.
