# Copy this file to `.env.local` and fill in the values before deploying.

# Convex configuration (populate after `npx convex dev`).
CONVEX_DEPLOYMENT=
CONVEX_URL=

# Authentication (Better Auth)
BETTER_AUTH_SECRET=
ADMIN_EMAIL=
ADMIN_PASSWORD=
CONVEX_AUTH_SECRET=
# Optional overrides
ADMIN_NAME=
AUTH_BASE_URL=
