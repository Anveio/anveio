{
  "name": "@anveio/clock",
  "version": "0.0.1",
  "private": true,
  "description": "Clock utilities implementing the Anveio ClockPort contract.",
  "sideEffects": false,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts",
      "require": "./src/index.ts"
    }
  },
  "scripts": {
    "clean": "git clean -xfd .",
    "lint": "biome lint .",
    "lint:fix": "biome lint --fix . && biome format --write .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --reporter=dot",
    "test:watch": "vitest --reporter=default --watch"
  }
}
